package cux.oracle.apps.per.bonus.project.server;

import cux.oracle.apps.per.bonus.advanceawards.server.AdvanceAwardsAMImpl;

import java.io.Reader;

import java.sql.CallableStatement;
import java.sql.Clob;
import java.sql.Types;

import oracle.apps.fnd.framework.server.OADBTransaction;
import oracle.apps.fnd.framework.server.OAViewObjectImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---    财务部，采购部部长 查询项目奖总体分配情况
// ---------------------------------------------------------------------
public class DeptPrjListVOImpl extends OAViewObjectImpl {
    /**This is the default constructor (do not remove)
     */
    public DeptPrjListVOImpl() {
    }

    public void initQuery(String PrjName, String StatusID) {
        String strSQL = this.getQuerySQL(PrjName, StatusID);
        cancelQuery();
        setQuery(strSQL);
        setMaxFetchSize(-1);
        executeQuery();
    }

    /* 获取查询SQL,
     * StatusID: 0 - 全部, 1 - 未完成, 2 - 已完成 */

    private String getQuerySQL(String PrjName, String StatusID) {
        ProjectAMImpl amImpl = (ProjectAMImpl)this.getApplicationModule();
        CallableStatement cs = null;
        OADBTransaction transaction = amImpl.getOADBTransaction();
        Clob cSQL = null;
        String strSQL = "";
        cs = 
 transaction.createCallableStatement("call APPS.CUX_HR_BONUS_PRJ_DEPT.GET_PRJDIST_DEPT_SQL(?, ?, ?)", 
                                     1);
        try {
            cs.setString(1, PrjName);
            cs.setString(2, StatusID);
            cs.registerOutParameter(3, Types.CLOB);
            cs.executeUpdate();
            cSQL = cs.getClob(3);
            Reader inStream = cSQL.getCharacterStream();
            char[] c = new char[(int)cSQL.length()];
            inStream.read(c);
            strSQL = new String(c);
            inStream.close();
            cs.close();
        } catch (Exception e) {
            System.out.println(e.toString());
        }
        return strSQL;
    }
}
