package cux.oracle.apps.per.bonus.advanceawards.server;

import java.io.Reader;

import java.sql.CallableStatement;
import java.sql.Clob;
import java.sql.Types;

import oracle.apps.fnd.framework.server.OADBTransaction;
import oracle.apps.fnd.framework.server.OAViewObjectImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class AdvAwardsOtherLovVOImpl extends OAViewObjectImpl {
    /**This is the default constructor (do not remove)
     */
    public AdvAwardsOtherLovVOImpl() {
    }

    public void initSQL(int deptOrgID, String distDate, 
                        String strEmployeeNumWhereClause) {
        String strSQL = getLovPerSQL(deptOrgID, distDate);
        setFullSqlMode(this.FULLSQL_MODE_AUGMENTATION);
        setWhereClause(null);
        setQuery(strSQL);
        if (!strEmployeeNumWhereClause.equals(""))
            setWhereClause(strEmployeeNumWhereClause);
        executeQuery();
        setMaxFetchSize(-1);
    }

    private String getLovPerSQL(int iDeptID, String strDistDate) {
        AdvanceAwardsAMImpl amImpl = 
            (AdvanceAwardsAMImpl)this.getApplicationModule();
        CallableStatement cs = null;
        OADBTransaction transaction = amImpl.getOADBTransaction();
        Clob cSQL = null;
        String strSQL = "";
        cs = 
 transaction.createCallableStatement("call CUX_HR_BONUS.GET_LOV_OTHER_SQL(?, ?, ?)", 
                                     1);
        try {
            cs.setInt(1, iDeptID);
            cs.setString(2, strDistDate);
            cs.registerOutParameter(3, Types.CLOB);
            cs.executeUpdate();
            cSQL = cs.getClob(3);
            Reader inStream = cSQL.getCharacterStream();
            char[] c = new char[(int)cSQL.length()];
            inStream.read(c);
            strSQL = new String(c);
            inStream.close();
            cs.close();
        } catch (Exception e) {
            System.out.println(e.toString());
        }
        return strSQL;
    }


}
