package cux.oracle.apps.per.bonus.advanceawards.server;

import java.io.Reader;

import java.sql.CallableStatement;
import java.sql.Clob;
import java.sql.Types;

import oracle.apps.fnd.framework.server.OADBTransaction;
import oracle.apps.fnd.framework.server.OAViewObjectImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class AdvAwardsSummaryOfficeVOImpl extends OAViewObjectImpl {
    /**This is the default constructor (do not remove)
     */
    public AdvAwardsSummaryOfficeVOImpl() {
    }
    //  WebFlag: 'QUERY',查询页面, 'SUMMARY',汇总页面

    public void initSQL(int ideptID, String distDate, String WebFlag) {
        String strSQL = getSummarySQL(ideptID, distDate, WebFlag, "INTERNAL");

        cancelQuery();
        setQuery(strSQL);
        setMaxFetchSize(-1);
        executeQuery();
    }

    /* 获取查询SQL */

    private String getSummarySQL(int iDeptID, String strDistDate, 
                                 String strWebFlag, String strDeptFlag) {
        AdvanceAwardsAMImpl amImpl = 
            (AdvanceAwardsAMImpl)this.getApplicationModule();
        CallableStatement cs = null;
        OADBTransaction transaction = amImpl.getOADBTransaction();
        Clob cSQL = null;
        String strSQL = "";
        cs = 
 transaction.createCallableStatement("call CUX_HR_BONUS.get_office_sum_sql(?, ?, ?, ?, ?)", 
                                     1);
        try {
            cs.setInt(1, iDeptID);
            cs.setString(2, strDistDate);
            cs.setString(3, strWebFlag);
            cs.setString(4, strDeptFlag);
            cs.registerOutParameter(5, Types.CLOB);
            cs.executeUpdate();
            cSQL = cs.getClob(5);
            Reader inStream = cSQL.getCharacterStream();
            char[] c = new char[(int)cSQL.length()];
            inStream.read(c);
            strSQL = new String(c);
            inStream.close();
            cs.close();
        } catch (Exception e) {
            System.out.println(e.toString());
        }
        System.out.println(strSQL);
        return strSQL;
    } //end getSummarySQL
}
